import styled from "styled-components";
import { tokens } from "@design/tokens";

const StyledConatinerAttribute = styled.div`
  display: flex;
  flex-direction: column;
  border-radius: ${tokens.spacing.s100};
  width: 100%;
`;

export { StyledConatinerAttribute };
